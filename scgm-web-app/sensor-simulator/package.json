{
  "name": "uwm-sensor-simulator",
  "version": "1.0.0",
  "description": "Sensor simulator for Urban Waste Monitoring",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node index.js",
    "lint": "npx @biomejs/biome lint --write",
    "format": "npx @biomejs/biome format --write",
    "check": "npx @biomejs/biome check --write"
  },
  "dependencies": {
    "axios": "^1.11.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2"
  }
}
